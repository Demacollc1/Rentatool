name: demaco
description: "DEMACO Rent a Tool — administración de renta de herramientas."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^16.0.0
  drift: ^2.28.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.5
  path: ^1.9.0
  uuid: ^4.5.1
  supabase_flutter: ^2.9.0
  intl: ^0.20.2
  pdf: ^3.11.3
  share_plus: ^12.0.0
  mobile_scanner: ^7.0.1
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.15.0
  url_launcher: ^6.3.1
  crypto: ^3.0.6
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  drift_dev: ^2.28.0
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
  assets:
    - assets/seed/portafolio.json
    - assets/seed/canonicos.json
